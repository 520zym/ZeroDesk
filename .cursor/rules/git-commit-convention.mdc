---
description: Git 提交信息规范，提交代码时必须遵循此规则
alwaysApply: true
---

# Git 提交信息规范

## 格式

```
改动类型(改动模块): 改动描述的摘要总结

【模块名】
- 具体改动点1
- 具体改动点2
```

## 改动类型

| 类型 | 说明 |
|------|------|
| feat | 新增功能 |
| improve | 增强已有功能或改善体验 |
| fix | 修复 Bug |
| refactor | 代码重构，不改变外部行为 |
| style | 纯 UI/CSS 调整 |
| docs | 文档更新 |
| chore | 构建/依赖/配置变更 |
| test | 测试相关 |

## 规则

1. **全部使用中文**，包括类型、模块名、描述
2. 摘要行不超过 50 字，简明扼要
3. 空一行后写详细改动，按**模块分组**，用【】包裹模块名
4. 每个改动点用 `- ` 开头，描述做了什么
5. 涉及多模块时，改动模块写 `多模块`
6. Windows PowerShell 不支持 HEREDOC，提交时**必须先写入临时文件再用 `git commit -F`**

## 示例

```
feat(模型管理): 新增系统内置模型配置与供应商管理弹窗

【模型与路由】
- 新增"系统内置模型"配置区
- 实现"添加供应商"弹窗
- 供应商卡片集成 API Key 显示
```

```
fix(侧边栏): 任务子页面菜单高亮不跟随切换

【侧边栏】
- 修复 /tasks/:id/console 路径下"执行控制台"不高亮的问题
- 重构 isActive 匹配逻辑，支持嵌套路由子页面
```
